{
  "name": "ZenSheet",
  "version": "0.0.1",
  "description": "ZenSheet website",
  "main": "web.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "dependencies": {
    "express": "~4.16.2"
  },

  "engines": {
    "node": "0.10.12",
    "npm": "1.2.30"
  }
}
